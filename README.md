

📝 কী হচ্ছে এখানে
remember { mutableStateOf(...) } দিয়ে প্রতিটি data type এর state রাখা হয়েছে।

UI‑তে Text দিয়ে current value দেখানো হচ্ছে।

প্রতিটি Button ক্লিক করলে state আপডেট হয় এবং UI আবার redraw হয়।

কোনো database (Room) ব্যবহার করা হয়নি — শুধু in‑memory state management।
